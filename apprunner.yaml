version: 1.0
runtime: python311
build:
  commands:
    build:
      - pip install --upgrade pip
      - pip install -r requirements.txt
      - python manage.py collectstatic --noinput
run:
  # command: gunicorn feijoadaweek.wsgi:application
  command: sh startup.sh
  network:
    port: 8080

